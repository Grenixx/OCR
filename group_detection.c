#include <SDL2/SDL.h>
#include <SDL2/SDL_image.h>

#include "stack.h"

void search_new_point_for_group(SDL_Surface *surface)
{
    int *is_visited = calloc(surface->w * surface->h, sizeof(int))
}

void flood_fill(SDL_Surface *surface, struct t_point *point_depart)
{
}